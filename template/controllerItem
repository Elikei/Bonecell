
  @sjp({
    api: '🦴',
    path: '/🍖',
    summary: '🍖',
    // query: schemas.S🍗In,
    body: schema.S🍗In,
    responses: schema.S🍗Out,
    description: '',
    auth: 'apiKey'
  })
  async 🍖(ctx: Context) {
    ctx.body = await this.🦴Service.🍖(ctx.request.body);
    // ctx.body = await this.🦴Service.🍖(ctx.query);
  }
